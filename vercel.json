{
    "rewrites": [
      {
        "source": "/(.*)",
        "destination": "/"
      }
    ],
    "buildCommand": "git lfs install && git lfs pull && next build",
    "ignoreCommand": "git diff --quiet HEAD^ HEAD ."
  }